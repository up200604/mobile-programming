import { StyleSheet, Text, View } from 'react-native';

function Home() {
    return (
        <View style={styles.main}>
            <Text>Home Page</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    main: {
        width: 'auto',
        height: 'auto',
        backgroundColor: {

        }
    }
});

export default Home;
